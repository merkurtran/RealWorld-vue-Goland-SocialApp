name: 'Setup Frontend Environment'
description: 'Sets up Node env and install frontend dependencies'

runs:
  using: 'composite'
  steps:
    - name: Set up Node
      uses: actions/setup-node@v4
      with:
        node-version: '25.9.0'
        cache: 'npm'
        cache-dependency-path: './frontend/package-lock.json'
    - name: Install Node Dependencies
      run: npm ci
      working-directory: ./frontend
      shell: bash

    - name: Build Vue App
      run: npm run build
      working-directory: ./frontend
      shell: bash